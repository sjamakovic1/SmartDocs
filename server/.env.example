# Connect to Supabase PostgreSQL via connection pooling.
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct database connection. Used for Prisma migrations.
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@[REGION].pooler.supabase.com:5432/postgres"

# Supabase server-side integrations. Never expose the service role key to the client.
SUPABASE_URL="https://[PROJECT-REF].supabase.co"
SUPABASE_SERVICE_ROLE_KEY="[YOUR-SERVICE-ROLE-KEY]"
SUPABASE_STORAGE_BUCKET="Documents"

PORT=5000
NODE_ENV=production

# Development only:
# The Vite client may set VITE_API_URL=http://localhost:5000/api.
# In production, leave VITE_API_URL unset so the built client uses same-origin /api.
